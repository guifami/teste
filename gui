private void filterOnlyScheduledItems(PixScheduleResponse response) {
    if (response == null || response.getSchedules() == null) {
        return;
    }

    response.getSchedules().removeIf(schedule -> schedule == null || schedule.getStatus() != ScheduleSituationPixEnum.AGENDADO);
}
